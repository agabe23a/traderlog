(()=>{const $=s=>document.querySelector(s), esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let market=new URLSearchParams(location.search).get('market')||'';let timer;
function fmt(v,sym){if(v==null||!Number.isFinite(Number(v)))return'—';const n=Number(v);const d=/JPY/.test(sym)?3:/BTC|ETH|AAPL|TSLA|SPX|NAS/.test(sym)?2:4;return n.toLocaleString(undefined,{minimumFractionDigits:d,maximumFractionDigits:d})}
function card(x){const q=x.quote||{}, st=q.data_status||'UNAVAILABLE', ch=Number(q.change_pct);const cc=Number.isFinite(ch)?ch>0?'tl-change-up':ch<0?'tl-change-down':'tl-change-flat':'tl-change-flat';return `<div class="col-sm-6 col-xl-3"><a href="/instrument/${encodeURIComponent(x.id)}" class="text-decoration-none"><article class="tl-market-card h-100"><div class="d-flex justify-content-between gap-2"><div><div class="tl-symbol">${esc(x.symbol)}</div><div class="tl-market-name">${esc(x.name||x.market_code)}</div></div><span class="tl-data-status ${st==='LIVE'?'tl-status-live':st==='STALE'?'tl-status-stale':'tl-status-unavailable'}">${esc(st)}</span></div><div class="tl-price mt-4">${fmt(q.price,x.symbol)}</div><div class="d-flex justify-content-between mt-2"><span class="${cc}">${Number.isFinite(ch)?(ch>0?'+':'')+ch.toFixed(2)+'%':'—'}</span><span class="tl-data-status">${q.fetched_at?new Date(q.fetched_at).toLocaleTimeString():'—'}</span></div>${st==='UNAVAILABLE'?`<div class="small tl-muted mt-3">${esc(q.reason||'Provider unavailable')}</div>`:''}</article></a></div>`}
async function load(){try{const r=await fetch('/api/market-overview?limit=32'+(market?'&market='+encodeURIComponent(market):''),{cache:'no-store'});const d=await r.json();if(!r.ok)throw Error(d.error||'Market request failed');const rows=d.data||[];$('#marketGrid').innerHTML=rows.map(card).join('')||'<div class="col-12"><div class="tl-card p-5 text-center tl-muted">No instruments in this market.</div></div>';$('#marketCount').textContent=rows.length;$('#liveCount').textContent=rows.filter(x=>x.quote?.data_status==='LIVE').length;$('#staleCount').textContent=rows.filter(x=>x.quote?.data_status==='STALE').length;$('#unavailableCount').textContent=rows.filter(x=>x.quote?.data_status==='UNAVAILABLE').length;$('#marketUpdated').textContent='UPDATED '+new Date(d.fetched_at).toLocaleTimeString()}catch(e){$('#marketGrid').innerHTML=`<div class="col-12"><div class="alert alert-warning">${esc(e.message)}</div></div>`}}
document.querySelectorAll('.tl-market-filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.tl-market-filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');market=b.dataset.market;load()}));$('#refreshMarkets')?.addEventListener('click',load);load();timer=setInterval(load,60000);window.addEventListener('beforeunload',()=>clearInterval(timer))})()
