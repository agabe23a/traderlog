(()=>{const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const symbols=['SPX500','NAS100','US30','RUT2000','VIX','AAPL','MSFT','NVDA','AMZN','META','TSLA','GOOGL'];function fmt(v){return v==null||!Number.isFinite(Number(v))?'—':Number(v).toLocaleString(undefined,{maximumFractionDigits:2})}async function load(){try{const r=await fetch('/api/market-overview?limit=80&market=INDICES');const idx=(await r.json()).data||[];const rs=await fetch('/api/market-overview?limit=80&market=STOCKS');const stocks=(await rs.json()).data||[];const all=[...idx,...stocks];const pick=s=>all.find(x=>x.symbol.toUpperCase()===s);$('#indexGrid').innerHTML=symbols.slice(0,5).map(s=>{const x=pick(s),q=x?.quote||{};return `<div class="col-6 col-md"><div class="tl-market-card"><div class="tl-symbol">${esc(s)}</div><div class="tl-price mt-3">${fmt(q.price)}</div><div class="${Number(q.change_pct)>=0?'tl-change-up':'tl-change-down'} mt-1">${q.change_pct!=null?Number(q.change_pct).toFixed(2)+'%':'—'}</div><div class="tl-data-status mt-2">${esc(q.data_status||'UNAVAILABLE')}</div></div></div>`}).join('');const movers=all.filter(x=>x.quote?.data_status!=='UNAVAILABLE').sort((a,b)=>Math.abs(Number(b.quote.change_pct||0))-Math.abs(Number(a.quote.change_pct||0))).slice(0,16);$('#movers').innerHTML=movers.map(x=>{const q=x.quote;return `<div class="col-sm-6"><a href="/instrument/${encodeURIComponent(x.id)}" class="tl-list-item d-flex justify-content-between p-3 rounded text-decoration-none"><span><strong>${esc(x.symbol)}</strong><small class="d-block tl-muted">${esc(x.name||x.market_code)}</small></span><span class="${Number(q.change_pct)>=0?'tl-change-up':'tl-change-down'}">${q.change_pct!=null?Number(q.change_pct).toFixed(2)+'%':'—'}</span></a></div>`}).join('')||'<div class="col-12 tl-muted">No stock/index quote data is available.</div>';$('#wallUpdated').textContent='UPDATED '+new Date().toLocaleTimeString()}catch(e){$('#movers').innerHTML=`<div class="col-12 alert alert-warning">${esc(e.message)}</div>`}}load()})()
